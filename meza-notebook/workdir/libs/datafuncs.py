import json

# 3.1 Starting ART default
# StartART_<1 (M) HV03-01
def is_EPupthjgHvW(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 0
    age_max = 1

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    print(records[0])    
    
    return True


# 3.1 Starting ART default
# StartART_<1 (F) HV03-02
def is_mvZ6HDD8FaI(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 0
    age_max = 1

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# StartART_1-4 (M) HV03-03
def is_QYHSfraZSfk(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 1
    age_max = 4

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# StartART_1-4 (F) HV03-04
def is_cbTy9mLysBY(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 1
    age_max = 4

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# StartART_5-9 (M) HV03-05
def is_bg6lIhba13W(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 5
    age_max = 9

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# StartART_5-9 (F) HV03-06
def is_KrzxLDTWPg0(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 5
    age_max = 9

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_10-14(M) HV03-018
def is_LC2OqnUC5Sn(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 10
    age_max = 14

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_10-14 (F) HV03-019
def is_jthpt5cVV9c(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 10
    age_max = 14

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_15-19(M) HV03-020
def is_yWkSi8L3qGm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 15
    age_max = 19

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_15-19 (F) HV03-021
def is_DaNcGZnkclz(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 15
    age_max = 19

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_20-24 (F) HV03-023
def is_zNCSlBKbS6d(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 20
    age_max = 24

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_20-24(M) HV03-022
def is_K59f8nZ5vhy(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 20
    age_max = 24

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_25+(M) HV03-024
def is_SCMKsiNj6c5(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 25
    age_max = 120

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.1 Starting ART default
# Start ART_25+ (F) HV03-025
def is_Nv4OkbdDvmm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 25
    age_max = 120

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_<1 (M) HV03-015
def is_MznGrMiRYoI(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 0
    age_max = 1

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_<1 (F) HV03-016
def is_OjfPFN6k49U(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 0
    age_max = 1

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_1-4 (M) HV03-017
def is_ZzqJfnuxQcr(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 1
    age_max = 4

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_1-4 (F) HV03-018
def is_LC2OqnUC5Sn(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 1
    age_max = 4

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_5-9(M)HV03-019
def is_jthpt5cVV9c(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 5
    age_max = 9

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# OnART_5-9 (F) HV03-020
def is_yWkSi8L3qGm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 5
    age_max = 9

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_10-14(M) HV03-030
def is_Jbu4if6gtDp(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 10
    age_max = 14

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_10-14 (F) HV03-031
def is_Q1oSbJzJX0r(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 10
    age_max = 14

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_15-19(M) HV03-032
def is_oOOnacUi9Jm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 15
    age_max = 19

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_15-19 (F) HV03-033
def is_R1t8ZCVVzGA(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 15
    age_max = 19

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_20-24(M) HV03-034
def is_cPDPTqNmIRz(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 20
    age_max = 24

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_20-24 (F) HV03-035
def is_JLJwdUlAG6S(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 20
    age_max = 24

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_25+(M) HV03-036
def is_pR7VzBydoj3(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Male'
    age_min = 25
    age_max = 120

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.2 Currently on ART ([All] default
# On ART_25+ (F) HV03-037
def is_nGD7IkS32or(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    gender_valid = 'Female'
    age_min = 25
    age_max = 120

    # check requirements
    if gender != gender_valid: return False
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.3 TB Screening default
# ScreenforTB_<15 HV03-029
def is_ByXDJGz6DT4(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.3 TB Screening default
# ScreenforTB_15+ HV03-030
def is_Jbu4if6gtDp(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.4 Starting TPT default
# StartTPT_<15 HV03-031
def is_Q1oSbJzJX0r(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.4 Starting TPT default
# StartTPT_15+ HV03-032
def is_oOOnacUi9Jm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.5 Differentiated Service Delivery default
# Established HV03-033
def is_R1t8ZCVVzGA(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.5 Differentiated Service Delivery default
# Not Established HV03-034
def is_cPDPTqNmIRz(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.5 Differentiated Service Delivery default
# Community HV03-035
def is_JLJwdUlAG6S(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.5 Differentiated Service Delivery default
# Facility HV03-036
def is_pR7VzBydoj3(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 0-59Months HV03-037
def is_nGD7IkS32or(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 4

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 5-9Years HV03-038
def is_vwbg9CWtt9A(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 5
    age_max = 9

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 10-14yearsHV03-039
def is_Zv2sW71iTmB(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 10
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 15-19Years HV03-040
def is_R4TfYioXSxo(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 19

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 20+ HV03-041
def is_g7EitMO66Wt(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 20
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# Pregnant & Lactating HV03-042
def is_NbqCcHtB8i5(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 0-59Months HV03-043
def is_W61CI4pcols(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 4

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 5-9Years HV03-044
def is_r17Xme6fjlm(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 5
    age_max = 9

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 10_14Years HV03-045
def is_FnKBjLImniW(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 10
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 15-19Years HV03-046
def is_oxYTZiYoAW7(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 19

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 20+ HV03-047
def is_wft4GPSstKW(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 20
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# Pregnant & Lactating HV03-048
def is_lYMLRSDvKpS(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 0-59Months HV03-049
def is_BZQdaxIMmW1(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 4

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 5-9Years HV03-050
def is_Vtb5L6AGNTu(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 5
    age_max = 9

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 10-14years HV03-051
def is_xbyphzQa3gL(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 10
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 15-19Years HV03-052
def is_hJA5hxXi2AZ(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 19

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 20+ HV03-053
def is_Aw8hNe4FL2x(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 20
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# Pregnant & Lactating HV03-054
def is_R3lw5SzB2XK(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 0-59Months HV03-055
def is_EowokmpJJAp(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 0
    age_max = 4

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 5-9Years HV03-056
def is_P3Qob1L58EE(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 5
    age_max = 9

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 10-14years HV03-057
def is_fL9ikn7wMRl(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 10
    age_max = 14

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 15-19Years HV03-058
def is_gaAvAuCjbor(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 15
    age_max = 19

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# 20+ HV03-059
def is_bwDLJol0pdB(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # set requirements
    age_min = 20
    age_max = 120

    # check requirements
    if age < age_min or age > age_max: return False

    # check data
    
    
    return True


# 3.6 Nutrition and HIV default
# Pregnant & Lactating HV03-060
def is_g2JRk66BZs7(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.7 HIV in TB Clinic default
# TB cases _New HV03-076
def is_Vb9I1VRoaZt(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.7 HIV in TB Clinic default
# TB Cases _Known Positive(KPs) HV03-077
def is_TXJ3QlKmD10(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.7 HIV in TB Clinic default
# TB_New HIV Positive HV03-080
def is_jjXPL4ZIuIU(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.7 HIV in TB Clinic default
# TB_already on HAART HV03-082
def is_IOy5jgQqfHU(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True


# 3.7 HIV in TB Clinic default
# TB_start_HAART HV03-083
def is_PIxcR1T5ORh(reporting_year, reporting_month, gender, age, records):
    records = json.loads(records)
    
    # check data
    
    
    return True